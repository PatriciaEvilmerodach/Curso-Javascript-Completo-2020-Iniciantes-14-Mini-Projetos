// Existem 2 tipos primitivos: Os de Referência e os de Valores; abaixo, são os principais tipos de Valores:

let nomePiloto = 'Lewis Hamilton';
console.log(nomePiloto);
// String: utilizada para atribuir frases a variável; pode conter letras, números, espaços ou caracteres especiais

let titulosMundiais = 7;
console.log(titulosMundiais);
// Number: utilizado para atribuir números a variável; serve para realizar operações matemáticas

let equipeMercedes = true;
console.log(equipeMercedes);
// Boolean: utilizado para atribuir valores binários de "Verdadeiro" ou "Falso" para a variável

let posicaoCampeonato2024 = undefined;
console.log(posicaoCampeonato2024);
// Undefined: quando um valor não é atribuído a variável
// Podemos simplesmente escrever "undefined" após o operador de atribuição ou deixar em branco; o resultado no console será o mesmo.

let corCapacete = null;
console.log(corCapacete);
// Null: quando não definimos um valor para a variável, porém deixamos em aberto para que seja feita uma escolha por parte do usuário